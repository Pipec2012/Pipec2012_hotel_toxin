mixin like_button(options)

   .like_button_wrap
      .like_button__head
         div(class= options.title ? "like_button__title" : '' )= (options.title ? options.title : '')
      .like_button_body
         <i class="far fa-heart"></i>
         span.like__quantity 12